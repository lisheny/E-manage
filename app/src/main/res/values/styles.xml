<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="tab_bottom">
        <item name="android:ellipsize">marquee</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:button">@null</item>
        <item name="android:singleLine">true</item>
        <item name="android:layout_weight">1.0</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_margin">3dip</item>
    </style>

    <style name="MyDialogStyle">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- 自定义时间 Dialog 弹出样式(底部有动画效果) -->
    <style name="dialog_with_alpha" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <!-- <item name="android:backgroundDimEnabled">false</item> -->
    </style>

    <style name="progress_dialog_style" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- 按钮的颜色,进度对话框时，是进度条的颜色 -->
        <item name="colorAccent">@color/recharge_text_selector</item>
        <!-- 背景的颜色 -->
        <item name="android:background">@color/write</item>
        <!-- 字体的颜色 -->
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="alert_dialog_style" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- 按钮的颜色,进度对话框时，是进度条的颜色 -->
        <item name="colorAccent">@color/black</item>
        <!-- 标题，字体的颜色 -->
        <item name="android:textColorPrimary">@color/black</item>
        <!-- 背景的颜色 -->
        <item name="android:background">@color/write</item>
        <!-- 标题颜色 -->
        <item name="android:textColor">@color/black</item>
    </style>



</resources>
